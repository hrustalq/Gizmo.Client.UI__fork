@namespace Gizmo.Web.Components
@inherits CustomDOMComponentBase

<ul class="giz-pagination">
    <li @onclick="OnClickPreviousHandler" class="@(!IsPreviousEnabled ? "disabled" : "")">Previous</li>
    <li @onclick="OnClickNextHandler" class="@(!IsNextEnabled ? "disabled" : "")">Next</li>
</ul>